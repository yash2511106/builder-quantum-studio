# MongoDB Connection (Update with your MongoDB Atlas URI or local MongoDB)
MONGODB_URI=mongodb://localhost:27017/bias-detector

# JWT Secret (Change this in production!)
JWT_SECRET=bias-detector-jwt-secret-key-2024-change-in-production

# Environment
NODE_ENV=development
PORT=8080

# For MongoDB Atlas, use this format:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bias-detector
